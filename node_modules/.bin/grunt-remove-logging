#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-remove-logging/bin/grunt-remove-logging" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-remove-logging/bin/grunt-remove-logging" "$@"
  ret=$?
fi
exit $ret
